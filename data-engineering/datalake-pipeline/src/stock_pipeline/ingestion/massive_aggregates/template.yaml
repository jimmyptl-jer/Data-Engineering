AWSTemplateFormatVersion: "2010-09-09"
Transform: AWS::Serverless-2016-10-31
Description: >
  Massive Aggregates Ingestion Lambda
  Fetches grouped daily OHLCV aggregate data from the Massive (Polygon.io) API
  and lands raw JSON payloads into the S3 Bronze layer.

# ============================================================
# GLOBAL SETTINGS
# ============================================================

Globals:
  Function:
    Timeout: 600
    MemorySize: 512
    Runtime: python3.14
    Environment:
      Variables:
        LOG_LEVEL: INFO

# ============================================================
# PARAMETERS
# ============================================================

Parameters:
  EnvironmentName:
    Type: String
    Default: Development
    AllowedValues:
      - Development
      - QA
      - UAT
      - Production

  S3BucketName:
    Type: String
    Default: graywolf--data--lake
    Description: Target S3 data lake bucket (must already exist)

  StockSymbols:
    Type: String
    Default: "IBM"
    Description: Comma-separated stock symbols to ingest

  ScheduleExpression:
    Type: String
    Default: "cron(0 14 ? * MON-FRI *)"
    Description: EventBridge schedule expression for automatic runs

  EnableSchedule:
    Type: String
    Default: "true"
    AllowedValues:
      - "true"
      - "false"

  MassiveApiKey:
    Type: String
    NoEcho: true
    Description: Massive (Polygon.io) API key

# ============================================================
# CONDITIONS
# ============================================================

Conditions:
  IsScheduleEnabled: !Equals [!Ref EnableSchedule, "true"]

# ============================================================
# RESOURCES
# ============================================================

Resources:

  MassiveAggregatesIngestFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub "ingest-massive-aggregates-${EnvironmentName}"
      Handler: src.stock_pipeline.ingestion.massive_aggregates.app.lambda_handler
      # CodeUri points to the project root so the full src package is included
      CodeUri: ../../../../
      Description: Ingests Massive (Polygon.io) grouped daily OHLCV aggregates → S3 Bronze
      Architectures:
        - x86_64
      Tags:
        Project: AlphaVantage-DataLake
        Environment: !Ref EnvironmentName
        Owner: Jimmy Patel
        ManagedBy: AWS SAM
        Layer: Ingestion
        Source: Massive
      Environment:
        Variables:
          MASSIVE_API_KEY: !Ref MassiveApiKey
          S3_BUCKET_NAME: !Ref S3BucketName
      Policies:
        - Statement:
            - Sid: S3BronzeWriteAccess
              Effect: Allow
              Action:
                - s3:PutObject
                - s3:GetObject
                - s3:ListBucket
              Resource:
                - !Sub "arn:aws:s3:::${S3BucketName}"
                - !Sub "arn:aws:s3:::${S3BucketName}/*"
      Events:
        ScheduledRun:
          Type: Schedule
          Properties:
            Schedule: !Ref ScheduleExpression
            Description: Scheduled Massive aggregates ingestion run
            Enabled: !If [IsScheduleEnabled, true, false]
            Input: !Sub '{"stock_symbols": ["${StockSymbols}"]}'

  MassiveAggregatesIngestLogGroup:
    Type: AWS::Logs::LogGroup
    Properties:
      LogGroupName: !Sub "/aws/lambda/${MassiveAggregatesIngestFunction}"
      RetentionInDays: 30

# ============================================================
# OUTPUTS
# ============================================================

Outputs:
  FunctionName:
    Description: Massive aggregates ingestion Lambda function name
    Value: !Ref MassiveAggregatesIngestFunction

  FunctionArn:
    Description: Massive aggregates ingestion Lambda function ARN
    Value: !GetAtt MassiveAggregatesIngestFunction.Arn

  LogGroupName:
    Description: CloudWatch log group name
    Value: !Ref MassiveAggregatesIngestLogGroup
