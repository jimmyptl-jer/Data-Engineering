"""
Stock Pipeline Ingestion Package.

Contains API-specific ingestion modules for landing raw data
into the S3 Bronze layer.

Modules:
    alpha_vantage_ingestion  — Alpha Vantage REST API ingestion
    massive_ingestion        — Massive (Polygon.io) REST API ingestion
    finnhub_ingestion        — Finnhub REST API ingestion
"""
